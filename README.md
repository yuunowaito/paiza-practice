## 2026/08/06
- 問題1: C181 暗号の手がかり(文字列長のばらつき)
  - 使ったメソッド: split, each, max, min
  - 間違えた書き方: m.length.max - m.length.min (配列の要素数と各要素の文字数を混同)
  - 気づき: 複数の結果を溜めるときは空配列を用意してeachで1つずつ追加する

- 問題2: レベル戦闘シミュレーション
  - 使ったメソッド: split, map(&:to_i), times, if/elsif
  - 間違えた書き方: n.time(times の s 忘れ)、X と x の大文字小文字混同、end不足
  - 気づき: 複数代入(n, l = ...)、大文字小文字は別変数扱いになる、if/elsifは3パターン中「何もしない」パターンがあるときに使うと素直

  ## 2026/08/07
- 問題3: C144 じゃんけんの結果
  - 使ったメソッド: gets.to_i, n.times, gets.split(多重代入), if/elsif, &&, count += 1
  - 間違えた書き方: `=`(代入)と`==`(比較)の混同、文字列に引用符をつけ忘れ
  - 気づき: 条件式には必ず`==`、文字列リテラルは引用符必須

- 問題4: C177 会員グレード判定
  - 使ったメソッド: gets.split.map(&:to_i), each, if, q.sum, &&
  - 間違えた書き方: each内で毎回puts判定していた(ループの外で1回だけ判定すべきだった)
  - 気づき: splitは配列を返す、to_iは文字列用。mapが両者を橋渡しする。.sumで配列の合計が一発で出せる

  ## 2026/08/09
- 問題6: C149 大文字小文字(難易度1314±5)
  - 使ったメソッド: chars, each_with_index, ハッシュ, downcase, 文字列連結(+=)
  - 気づき: ハッシュはキーで値を管理できる。配列は数字、ハッシュは好きな値で場所を指定できる

- 問題7: 反復横跳び順位
  - 使ったメソッド: 二重ループ(each×2)、if、count += 1
  - 気づき: 外側ループの変数(record)は1周の間固定、内側ループの変数(r)は毎回全員分動く

## 保留中
- C123:節分ロボット 節分の豆配り問題: 二次元配列、範囲ループ((a-1..b-1))、.minでのクランプ処理でつまずき保留

## 2026/08/10
- 問題8: C138 エレベーター移動距離
  - 使ったメソッド: .abs, +=, 状態変数と積算変数の分離
  - 気づき: 「置き換えるもの」と「積み上げるもの」は別の変数に分ける

- 問題9: パーフェクトシャッフル
  - 使ったメソッド: card[開始位置,個数], zip, flatten, join(" ")
  - 気づき: ルールが明記されてない問題は、出力例から逆算して手計算で規則を確認してから実装する

  ## 2026/08/11
- 問題10: パーフェクトシャッフル
  - 使ったメソッド: card[開始位置,個数], zip, flatten, join(" ")
  - 気づき: ルールが明記されてない問題は、出力例から逆算して手計算で規則を確認してから実装する

- 問題11: ボールのパス回し
  - 使ったメソッド: 配列インデックス操作, .min, +=/-=
  - 気づき: 「人の番号(1始まり)」と「配列の添字(0始まり)」のズレは a-1 で変換する

- 問題12: paizaカードゲーム(ハイアンドロー)
  - 使ったメソッド: if/elsif/elseの入れ子, &&
  - 気づき: elseの中は外側の条件が否定されたことが確定済みなので、条件を省略できる